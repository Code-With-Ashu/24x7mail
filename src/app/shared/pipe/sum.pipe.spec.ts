import {FieldSumPipe} from './sum.directive';

describe('SumDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldSumPipe();
    expect(directive).toBeTruthy();
  });
});
