import {DateYYMMDDPipe} from './date-yymmdd.pipe';

describe('DateYYMMDDPipe', () => {
  it('create an instance', () => {
    const pipe = new DateYYMMDDPipe();
    expect(pipe).toBeTruthy();
  });
});
