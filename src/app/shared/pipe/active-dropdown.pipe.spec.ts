import {ActiveDropdownPipe} from './active-dropdown.pipe';

describe('ActiveDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
